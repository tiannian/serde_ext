[package]
name = "toml_ext"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
toml = "1.1.2"

[dev-dependencies]
serde_bytes = "0.11"
